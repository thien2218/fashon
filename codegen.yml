overwrite: true
schema: ${PUBLIC_GRAPHQL_URL}
documents: "src/**/*.gql"
generates:
  src/lib/graphql/generated.d.ts:
    plugins:
    - typescript
    - typescript-operations
    - typed-document-node
    config:
      useTypeImports: true