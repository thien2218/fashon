query GetBooks($limit: Int) {
	books(limit: $limit) {
		id
		title
		price
		quantity
		authors
		allow_borrow
		cover_url
		reviews_aggregate {
			aggregate {
				count
				avg {
					rating
				}
			}
		}
	}
}
